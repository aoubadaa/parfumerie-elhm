ELHM INTERACTIVE MOBILE UPGRADE

Added without changing product IDs, cart totals, shipping, checkout or quiz logic:
- swipeable quick filters by scent family
- live product count + reset
- favorites stored locally in the browser
- mobile catalog swipe progress
- product-card micro-interactions
- touch swipe between the two product images
- polished add-to-cart feedback + toast
- cart image/layout mobile fix (contain, no cropping)
- swipe-down cart close from the cart header on mobile
- scroll reveal effects with reduced-motion support
- compact back-to-top button
- improved tap targets, focus states and mobile metadata

IMPORTANT:
Keep your existing image/homme/, image/femme/ and images/banner.png files.
The ZIP only contains placeholder folders so your image collection is not overwritten.
